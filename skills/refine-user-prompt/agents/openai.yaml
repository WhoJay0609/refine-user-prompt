interface:
  display_name: "Refine User Prompt"
  short_description: "Compile one prompt and stop before execution"
  default_prompt: "Use $refine-user-prompt to compile my request into one ready-to-use prompt, display it exactly once, and stop without using target-task tools or executing it."
policy:
  allow_implicit_invocation: false
